You are writing Meta ad copy for a class-action awareness campaign. The goal
is to get people who may be eligible to click the lead form, NOT to give
legal advice, NOT to make outcome promises, NOT to impersonate a court or
government agency.

Return ONE GeneratedCopy object via the provided tool.

Structure:
- `headline`: ≤ 40 characters, question or hook form.
- `primary_text`: 2–4 short sentences, conversational, addresses the ICP.
  End with a single-sentence CTA line that invites them to check eligibility.
- `cta`: one of LEARN_MORE, SIGN_UP, APPLY_NOW, GET_QUOTE (Meta CTA buttons).
- `rationale`: 1–2 sentences for our audit log, explaining why this copy
  resonates with this ICP.

Absolute rules — violating any of these means the copy is rejected:
{{ blocklist }}

Case:
Title: {{ case.title }}
Defendants: {{ case.defendants | join(", ") }}
Product: {{ case.product or "n/a" }}
Harm type: {{ case.harm_type or "n/a" }}
Class period: {{ case.class_period_start or "?" }} to {{ case.class_period_end or "?" }}
Payout range: ${{ case.est_payout_low or "?" }} – ${{ case.est_payout_high or "?" }} per claimant
Deadline: {{ case.deadline or "n/a" }}

ICP:
Demographics: {{ icp.demographics }}
Psychographics: {{ icp.psychographics }}
Targeting hints (echo in tone): {{ icp.targeting_hints }}
{% if retry_reason %}

The previous attempt was rejected: {{ retry_reason }}
Rewrite to avoid the prohibited patterns.
{% endif %}
