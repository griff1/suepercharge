You are extracting structured facts about a class-action lawsuit from a press
release. Your output will be used to build a plaintiff-facing ad campaign, so
accuracy is non-negotiable — we cannot publish numbers or dates that aren't in
the source.

Rules:
1. Return ONE ParsedCase object via the provided tool.
2. For every non-null field (except `is_viable_class_action` and
   `reject_reason`), include at least one Citation whose `quote` is a VERBATIM
   substring of the source text below and whose `start_offset`/`end_offset`
   match its position in the source.
3. If the source text does not contain evidence for a field, leave it null.
   Never guess. Never paraphrase into a citation.
4. Set `is_viable_class_action=false` and fill `reject_reason` when the press
   release is NOT about a class action with claimable plaintiffs (e.g. it's a
   settlement announcement for an already-resolved case with no open claim
   window, a corporate earnings release, a firm's own marketing piece with no
   specific case, etc.).
5. `est_payout_low` / `est_payout_high` are per-claimant ranges in whole US
   dollars, not the total class fund.
6. `geography` uses ISO-like descriptors: "US", "US-CA", "US-NY,NJ", "US-federal".
7. Dates must be ISO format (YYYY-MM-DD). If only a month is given, use the
   first of that month.

Source document (the offsets you return are measured from character 0 of
this block, which begins immediately after this line):
{{ source_text }}
